using UnityEngine;
using System.Collections;

public class LevelScale : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//float faktor = Screen.width / 1280f;
		//transform.localScale = new Vector3 (faktor*100f,faktor*100f,1);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
